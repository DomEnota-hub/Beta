# Dev14 Android emulator QA handoff — 2026-09-24

## Точка остановки

Эмулятор Android API 34 в GitHub Actions успешно поднимается и доступен через `adb`.
Приложение собирается, устанавливается и запускается на виртуальном Pixel 6. Полный smoke-маршрут завершён успешно на `font_scale = 1.3`: приёмка, диагностика, первая помощь, Атлас и финальная проверка logcat пройдены.

Актуальная версия приложения на момент проверки:

- `versionCode = 199`;
- `versionName = 1.2.2-dev14-beta50.3`.

Workflow переведён только на `workflow_dispatch`, поэтому новые push-коммиты не запускают дорогой эмулятор автоматически.

## Проверенная схема запуска

Файл workflow: `.github/workflows/dev14-emulator-smoke.yml`.

Подтверждённо работает следующая конфигурация:

1. `ubuntu-latest` и таймаут 45 минут.
2. JDK 17 через `actions/setup-java@v4`.
3. Android SDK Platform 34 и Build Tools 34.0.0 через `sdkmanager`.
4. Gradle 8.7 через `gradle/actions/setup-gradle@v4`.
5. Распаковка `RailBrakeCalculator.zip` в `buildsrc` и наложение `patch/`.
6. Сборка `:app:assembleDebug`.
7. Выдача `/dev/kvm` режима `0666` через правило udev.
8. `reactivecircus/android-emulator-runner@v2` со следующими параметрами:
   - API 34;
   - `x86_64`;
   - `google_apis`;
   - профиль `pixel_6`;
   - `-no-snapshot -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim`;
   - отключённые системные анимации.
9. В поле `script` должна быть одна команда:

   ```bash
   bash "$GITHUB_WORKSPACE/qa/run_emulator_smoke_ci.sh"
   ```

   Runner выполняет отдельные строки `script` как отдельные shell-команды, поэтому многострочная конструкция с переносами здесь ломается.
10. Обёртка `qa/run_emulator_smoke_ci.sh` запускает `qa/dev14_android_smoke.py`, а затем всегда сохраняет сведения об устройстве и полный logcat.
11. Шаг `actions/upload-artifact@v4` с `if: always()` сохраняет `qa-evidence/`, даже если smoke завершился ошибкой.

## Как запускать без браузера

Прямой запуск через браузер и авторизация браузерной сессии не нужны.

Использовать уже проверенный временный trigger-workflow:

1. Fresh-fetch `dev14-integration` и зафиксировать source HEAD.
2. Создать временный workflow, запускающийся по push только собственного файла.
3. Выдать ему:

   ```yaml
   permissions:
     contents: read
     actions: write
   ```

4. Перед dispatch проверить:
   - `HEAD^` совпадает с ожидаемым source HEAD;
   - `HEAD` совпадает с `origin/dev14-integration`;
   - ожидаемые `versionCode` и `versionName` находятся в `app` и `patch/app`.
5. Выполнить:

   ```bash
   gh workflow run dev14-emulator-smoke.yml --ref dev14-integration
   ```

   с `GH_TOKEN: ${{ github.token }}`.
6. Дождаться именно `Dev14 emulator smoke`, скачать `dev14-emulator-smoke-evidence`, записать run ID, artifact ID и source SHA.
7. Удалить временный trigger-workflow и fresh-fetch итоговый HEAD.

## Исправленные проблемы инфраструктуры

1. Многострочное поле `script` распадалось на отдельные команды. Логика вынесена в `qa/run_emulator_smoke_ci.sh`.
2. Первый `uiautomator dump /dev/tty` на API 34 мог вернуть пустой ответ. Теперь UI-дерево сохраняется через `/sdcard/qa-window.xml` с повторными попытками.
3. На чистом эмуляторе `settings get system font_scale` возвращает `null`. Это значение теперь трактуется как `1.0`.
4. `cmd package resolve-activity --brief` на Android 14 печатает строку метаданных перед компонентом. Скрипт выбирает только строку формата `package/Activity`.
5. Кнопка `?` при шрифте 130% уходит за правый край горизонтальной панели приёмки. Smoke умеет прокрутить панель до неё, но сам факт скрытия зафиксирован как UX-дефект.
6. Одного вертикального свайпа недостаточно, чтобы дойти до последнего абзаца справки. `scroll_up_until_visible` выполняет до четырёх свайпов до появления целевого текста.
7. При 130% элементы ниже границы `LazyColumn` отсутствуют в UI-дереве до прокрутки. Сценарий прокручивает каждый экран к целевому элементу перед нажатием.
8. Системные диалоги `System UI isn't responding` и `Pixel Launcher isn't responding` могут появляться повторно после холодного старта. `wait_for` и `tap` нажимают `Wait` и повторяют исходное действие.
9. Проверка шага приёмки использует полное фактическое значение `Шаг 1 из 81`, затем отдельно прокручивает к статусу и кнопке `Проверено`.
10. Поиск первой помощи закрывает клавиатуру и последовательно прокручивает к счётчику, карточке `Не дышит / СЛР` и блоку `Что делать`.
11. Маршрут Атласа учитывает промежуточный экран `серия → тип материала` и открывает каталог через кнопку `Оборудование`.

## Выполненные прогоны

| Run | ID | Результат | Что подтверждено |
| --- | ---: | --- | --- |
| 1 | `35953625281` | failure | API 34, KVM и Pixel 6 запускаются; обнаружена проблема многострочного `script`. |
| 2 | `35953997788` | failure | Shell-обёртка работает; обнаружен нестабильный первый UI-dump. |
| 3 | `35954480188` | failure | Установка APK работает; выявлен многострочный ответ `resolve-activity`. |
| 4 | `35954854754` | failure | Приложение и раздел «Приёмка» открываются; при 130% кнопка `?` скрыта справа. |
| 5 | `35955324696` | failure | Панель прокручивается, справка открывается, её содержимое реально прокручивается, кнопка «Понятно» остаётся закреплённой. Один свайп не достигает последнего абзаца. |
| 6–14 | разные | failure | По evidence последовательно устранены точные ожидания текста, прокрутка к невидимым элементам, повторные системные ANR-диалоги, клавиатура и промежуточный экран Атласа. Дефектов приложения и `FATAL EXCEPTION` не обнаружено. |
| 15 | `35990421335` | success | Полный маршрут на Pixel 6 / API 34 / 130% завершён; все проверки и загрузка evidence прошли. |

Успешный evidence run 15:

- artifact ID: `10804500841`;
- artifact: `dev14-emulator-smoke-evidence`;
- source SHA: `bafb7d6c28b533a01f31a021d6e836e2c0b462b6`;
- SHA-256 ZIP: `c5cc6ffda0ba377d04fad1b8bb6c94e9eae425133b42b53aa6eac5d40c42d579`;
- safe build, автоматически запущенный изменением `qa/**`: run `35990409754`, success.

## Подтверждённые результаты интерфейса

- На виртуальном Pixel 6 с `font_scale = 1.3` карточки приёмки отображаются без аварии.
- Справка Beta50.3 открывается в отдельном диалоге.
- Заголовок «Настройка приёмки» и кнопка «Понятно» закреплены.
- После свайпа UI-дерево меняется с абзацев 1–4 на абзацы 3–6: вертикальная прокрутка действительно работает.
- При 130% шрифте переключатель «Сохранять» занимает остаток строки, а кнопка `?` полностью уходит за правый край горизонтальной панели. Это первая запись будущего визуального аудита, предварительно P2.
- Последний абзац справки достигается прокруткой, а кнопка «Понятно» остаётся доступной.
- Пункт полного осмотра переводится в состояние «Проверено».
- Переключение диагностики ВЛ80С → Ермак → ВЛ80С работает.
- Поиск `cpr` возвращает одну карточку «Не дышит / СЛР» и открывает блок действий.
- Атлас открывает оборудование ВЛ80С через выбор материала, карточку главного выключателя, хлебную крошку и «Недавние материалы».
- В logcat успешного прогона нет `FATAL EXCEPTION`.

## Контроль успешного evidence

Artifact `10804500841` скачан и отдельно проверен после завершения workflow. Контроль выполнен без повторного запуска эмулятора.

- SHA из `source-sha.txt`: `bafb7d6c28b533a01f31a021d6e836e2c0b462b6` — совпадает с source SHA run 15.
- Версия из `version.txt`: `versionCode = 199`, `versionName = 1.2.2-dev14-beta50.3`.
- Устройство из `device.txt`: Pixel 6, `1080x2400`, density `420`, исходный системный `font_scale = 1.0`; smoke затем устанавливает `1.3`.
- Просмотрены все семь итоговых PNG и соответствующие UI XML.
- В проверенных UI XML нет пользовательских значений вида `snake_case`; единственная введённая латиница — тестовый запрос `cpr`.
- В обоих logcat нет `FATAL EXCEPTION`, `am_crash`, `am_anr`, `ANR in ru.railbrake.calculator` или другой ошибки процесса приложения.

Дополнительные записи будущего визуального аудита при шрифте 130%:

| Экран | Проблема | Причина | Приоритет | Предлагаемое исправление |
| --- | --- | --- | --- | --- |
| Приёмка | Кнопка `?` скрывается справа; переключатель виден лишь частично до горизонтальной прокрутки | Панель управления шире экрана, но не показывает, что её можно сдвинуть | P2 | Перекомпоновать панель в две строки или оставить справку постоянно видимой вне горизонтального списка |
| Приёмка, шаг проверки | Третий вариант статуса начинается за правой границей экрана | Три крупные кнопки состояния размещены в одной горизонтальной строке | P2 | Переносить действия на две строки либо заменить их компактной группой с явным индикатором прокрутки |
| Диагностика | Крайние режимы и фильтры выглядят обрезанными | Горизонтальные группы при 130% не имеют визуальной подсказки продолжения | P2 | Добавить градиент/индикатор продолжения или адаптивный перенос |
| Атлас | Вкладка «Статьи» частично уходит за правый край | Три вкладки не помещаются при увеличенном шрифте | P2 | Сделать вкладки адаптивными или явно обозначить горизонтальную прокрутку |

Поиск первой помощи, карточка СЛР, хлебные крошки и недавние материалы Атласа, а также состояние «Проверено» у приёмки визуально подтверждены. Новых P0/P1 в этом smoke-маршруте не найдено.

## Что осталось

1. Расширить read-only аудит на:
   - светлую и тёмную темы;
   - доступные палитры;
   - системный шрифт 100% и 130%;
   - пустые списки и поиск без результатов;
   - длинные названия и длинные списки;
   - отключённые пункты, замечания и «не применяется»;
   - основные прямые и обратные навигационные маршруты.
2. Составить единый read-only отчёт в формате `экран → проблема → причина → приоритет → предлагаемое исправление`.
3. После утверждения отчёта собрать исправления одним пакетом Beta50.4.

## Важное ограничение

Эмуляторный workflow не изменяет содержимое или механизм доступа скрытого раздела. При полном аудите скрытый раздел проверяется только визуально и навигационно в пределах ранее согласованного объёма.
